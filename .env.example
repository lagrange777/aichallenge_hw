# Copy the variable names into your shell environment. The application does not
# read .env files automatically, so a real key never needs to live in the repo.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.3-codex
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_TIMEOUT=2m
